<p><a href="{{ path('gt.catalog.classificators.import_form') }}">Import</a></p>

{{ form(form) }}

<table>
{% for c in classificators %}
    <tr>
        <td>{{ c.code }}</td>
        <td>{{ c.group.name }}</td>
        <td><a href="#">TODO</a></td>
    </tr>
{% endfor %}
</table>
